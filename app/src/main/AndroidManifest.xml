<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sayhi">

    <application
        android:name=".SayHi_Offline"
        android:allowBackup="true"
        android:icon="@mipmap/hi_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hi_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity
            android:name=".DisplayUserInfoToOthers"
            android:parentActivityName=".Search_people" />
        <activity android:name=".Search_people" />
        <activity android:name=".signUp" />
        <activity android:name=".signIn" />
        <activity android:name=".PersonalChat" />
        <activity android:name=".MainActivity" />
        <activity android:name=".startup_animation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>